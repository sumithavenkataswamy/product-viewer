import React from 'react';
import PageLayout from '../page-layout/PageLayout';

const Companies: React.FC = () => {
  return <PageLayout
    title="Companies Management" children={undefined}>
  </PageLayout>
};

export default Companies;
